[project]
name = "for-my-stock"
version = "0.1.0"
description = "U.S. macro impact and market data pipeline"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.15,<3",
    "psycopg[binary]>=3.2,<4",
    "pyspark==4.2.0",
    "websockets>=15,<16",
]
