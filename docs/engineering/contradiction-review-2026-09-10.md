# Documentation contradiction review — 2026-09-10

## Review method

Compared the canonical contract against `README.md`, current architecture, migrations `001..008`, `src`, `dags`, tests, presentation documents and non-evidence Markdown. Historical evidence content was not rewritten.

## Resolved contradictions

| Area | Contradiction | Resolution |
| --- | --- | --- |
| identity | older pages frame automated trading, anomaly/news/LLM or an MVP as the active product | canonical identity is Economic Event Intelligence & Strategy Validation Platform; older pages carry a superseded banner |
| architecture | prior diagrams can appear current | README now labels the new Archify artifact as target and links the implemented-current page separately |
| event values | one compatibility row looks like a revision/consensus model | current/target matrix states the limitation; normalized additive schema is a foundation only |
| sessions | release-relative windows look exchange-calendar aware | legacy metrics remain legacy; verified-session planner is separate and not wired into analytics |
| quality | request success and observed coverage can both appear as COMPLETE | four axes are canonical and executable vocabulary; historical labels remain unchanged |
| execution | manual Paper journal can look like automatic strategy execution | current docs identify manual approval and isolation; experiment lineage remains target-only |
| deployment | a local web page can look like a deployed production product | target deployment contract lists auth/TLS/backups/managed services as unimplemented |
| configuration | target settings can look like active runtime capability | configuration contract separates current environment boundaries, P1 pure inputs and target-only families |
| dated presentation | 09.07 script and serving assignment use current tense | both now carry an explicit historical banner and link to the canonical current/target documents |
| provider choice | the 2026-08 MVP API choice can look like today's contract | API selection now carries a historical banner; current provider boundaries live in canonical contracts |

## Intentionally preserved history

Existing `docs/evidence/**`, dated assignment documents, archived scripts and legacy design bodies retain their original claims and numbers. The documentation hub classifies them as evidence/history. A superseded banner changes how a page is interpreted without falsifying its contents. The only new evidence directory is `docs/evidence/architecture-pass-2026-09-10/`; it records this pass and does not modify prior runs.

## Residual wording policy

Words such as “자동매매”, IEX, Streamlit, LLM and exactly-once still occur in historical or boundary documents. Their presence is acceptable only when the page is classified as historical, says the feature is not implemented, or explicitly rejects a blanket guarantee. Any new current-state page must use the canonical contract vocabulary.
